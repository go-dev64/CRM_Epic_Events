import pytest
from tests.factory.user_factory import ManagerFactory, SellerFactory, SupporterFactory


"""class TestLogin:
    # creer un test qui verifie l'authentification de l'user avec des bon id.

    def test_login_with_good_credentials(self):
        user = ManagerFactory()
        print(user)"""
